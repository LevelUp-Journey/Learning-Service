spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  
  h2:
    console:
      enabled: true

jwt:
  # Base64 encoded secret for testing (must be at least 512 bits for HS512)
  # This should match the secret used by the Auth Service in tests
  secret: d3ee61e796a04895924719559f19a19de2e31d4c330208d8c410c2d63c89de9b5aadc298404a843d521569e812c599bda47b65d4c7fce5a8ded21f24d8df59dc

application:
  max-authors: 5
  pagination:
    default-page-size: 10
    max-page-size: 100

logging:
  level:
    com.levelupjourney: DEBUG
    org.springframework.web: DEBUG
